# Mismos nombres que el .env real, sin los valores.
# Copiá este archivo como .env y pegá tus URLs de Supabase.

# Conexión por el pooler (puerto 6543) — la que usa la app
DATABASE_URL=""

# Conexión directa (puerto 5432) — la que usan las migraciones
DIRECT_URL=""

PORT=3010